import React from 'react';
import { Box, Typography, Paper } from '@mui/material';

const ScheduleAppointments = () => (
  <Paper elevation={2} sx={{ p: 4, background: '#fff' }}>
    <Typography variant="h4" color="primary" gutterBottom>Schedule Appointments</Typography>
    <Typography variant="body1">Schedule and manage appointments. Add your functionality here.</Typography>
  </Paper>
);

export default ScheduleAppointments; 